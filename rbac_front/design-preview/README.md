# Aegis 静态前端设计参考

直接用浏览器打开 `index.html` 即可预览。页面包含总览、用户管理、角色管理、权限配置、审计日志和登录页；侧边导航与登录入口可在这些静态页面之间跳转。

所有用户、角色、日志及统计数据均为虚构示例。搜索、筛选、创建、保存等控件只展示界面，不连接接口。当前设计采用固定侧边导航，以 `#6367FF`、`#8494FF`、`#C9BEFF`、`#FFDBFD` 为主要色彩；在窄屏下导航会固定在顶部。样式集中在 `style.css`，可以作为后续 Vue 页面实现的视觉参考。字体会尝试从 Google Fonts 加载；无法连接时会使用系统字体。
